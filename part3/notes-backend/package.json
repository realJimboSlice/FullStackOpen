{
  "name": "notes-backend",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": "Jimbo Slice",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:frontend": "cd ../notes && npm run build",
    "copy:frontend": "shx rm -rf dist && shx cp -r ../notes/dist ./dist",
    "build:ui": "npm run build:frontend && npm run copy:frontend",
    "deploy:full": "npm run build:ui && git add . && git commit -m \"uibuild\" && git push"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "shx": "^0.4.0"
  }
}
